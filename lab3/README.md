# Lab 03: Product Management System (ระบบจัดการสินค้า)

โปรแกรมจำลองระบบจัดการสินค้าที่พัฒนาด้วยภาษา Dart โดยประยุกต์ใช้หลักการ Object-Oriented Programming (OOP) ได้แก่ Encapsulation, Inheritance และ Polymorphism

## 📋 รายละเอียดโครงสร้างโค้ด (Code Description)

### 1. Class แม่: Product
เป็นคลาสหลักที่เก็บคุณสมบัติพื้นฐานของสินค้าทุกประเภท โดยมีการใช้ **Encapsulation** เพื่อปกป้องข้อมูลสำคัญ:
* [cite_start]**Private Property (`_id`):** กำหนด `_id` เป็น private เพื่อป้องกันการแก้ไขค่าโดยตรงจากภายนอก และสร้าง `getter` เพื่อให้อ่านค่าได้อย่างเดียว [cite: 12]
* [cite_start]**Validation via Setter:** ตัวแปร `price` ถูกควบคุมผ่าน `setter` เพื่อตรวจสอบความถูกต้อง โดยจะป้องกันไม่ให้กำหนดราคาสินค้าเป็นค่าติดลบหรือ 0 [cite: 20]
* [cite_start]**Method `restock()`:** สำหรับเพิ่มจำนวนสินค้า โดยมี Logic จัดการกรณีที่สินค้ายังไม่มีสต็อก (null) ให้เริ่มนับจาก 0 [cite: 26]
* [cite_start]**Method `applyDiscount()`:** คำนวณราคาสินค้าใหม่ตามเปอร์เซ็นต์ส่วนลดที่ระบุ [cite: 23]

### 2. Class ลูก: DigitalProduct (สืบทอดจาก Product)
* **Inheritance:** สืบทอดคุณสมบัติทั้งหมดมาจากคลาส `Product`
* [cite_start]**Extension:** เพิ่มคุณสมบัติเฉพาะคือ `fileSizeMB` (ขนาดไฟล์) [cite: 34]
* [cite_start]**Overriding:** ทำการ Override เมธอด `showInfo()` ให้แสดงข้อมูลพื้นฐานจากแม่ และแสดงข้อมูลเฉพาะ (Type: Digital และ File Size) เพิ่มเติม [cite: 36]

### 3. Class ลูก: FoodProduct (สืบทอดจาก Product)
* **Inheritance:** สืบทอดคุณสมบัติทั้งหมดมาจากคลาส `Product`
* [cite_start]**Extension:** เพิ่มคุณสมบัติเฉพาะคือ `expireDate` (วันหมดอายุ) [cite: 39]
* [cite_start]**Overriding:** ทำการ Override เมธอด `showInfo()` ให้แสดงข้อมูลพื้นฐานจากแม่ และแสดงข้อมูลเฉพาะ (Type: Food และ Expire Date) เพิ่มเติม [cite: 41]

### 4. การทำงานหลัก (main) และ Polymorphism
* มีการสร้างสินค้าทั้ง 3 รูปแบบ (`Product`, `DigitalProduct`, `FoodProduct`)
* **Polymorphism Implementation:** เก็บวัตถุทั้งหมดลงใน `List<Product>` และวนลูปสั่งคำสั่ง `showInfo()`
* ผลลัพธ์คือโปรแกรมสามารถเรียกใช้เมธอดที่ถูกต้องของวัตถุแต่ละประเภทได้เอง (เช่น เรียก `showInfo` ของ DigitalProduct เมื่อเจอสินค้าดิจิทัล)

---

## 💻 Source Code

```dart
void main() {
  // 4) Polymorphism - การทำงานใน main()
  print('--- เริ่มต้นโปรแกรม ---');

  // 4.1 สร้างสินค้าอย่างน้อย 3 ชิ้น (Product, DigitalProduct, FoodProduct)
  // ชิ้นที่ 1: สินค้าทั่วไป (Product)
  Product p1 = Product(id: 'P001', name: 'Notebook', price: 50.0, stock: 50);
  print('New Product created: ${p1.id} (${p1.name})');

  // ชิ้นที่ 2: สินค้าดิจิทัล (DigitalProduct)
  DigitalProduct d1 = DigitalProduct(
    id: 'D001',
    name: 'E-Book Flutter',
    price: 199.0,
    fileSizeMB: 120.5,
    // ไม่ใส่ stock เพื่อทดสอบ case stock = null (ยังไม่ระบุ)
  );
  print('New Product created: ${d1.id} (${d1.name})');

  // ชิ้นที่ 3: สินค้าอาหาร (FoodProduct)
  FoodProduct f1 = FoodProduct(
    id: 'F001',
    name: 'Milk',
    price: 25.0,
    stock: 15,
    expireDate: '2026-01-10',
  );
  print('New Product created: ${f1.id} (${f1.name})');

  print('\n--- ทดสอบ Setter Price (ป้องกันค่าติดลบ) ---');
  // 4.3 ทดลองตั้งราคาเป็นค่าติดลบ
  p1.price = -500; // ควรแสดงข้อความเตือนและไม่เปลี่ยนค่า

  print('\n--- ทดสอบ Method ต่างๆ ---');
  // 4.2 ทดสอบลดราคาและเพิ่มสต็อก
  print('ลดราคา Notebook 10%');
  p1.applyDiscount(10); // ลด 10%

  print('เพิ่มสต็อก Milk +50');
  f1.restock(50); // เพิ่มของ

  print('\n------------------------------------------------');
  print('--- แสดงผลข้อมูลสินค้าทั้งหมด (Polymorphism) ---');
  print('------------------------------------------------');

  // 4. Polymorphism: เก็บลง List
  List<Product> productList = [p1, d1, f1];

  // วนลูปเรียก showInfo()
  for (var item in productList) {
    item.showInfo();
    print('----------------');
  }
}

// ---------------------------------------------------------
// 1) Class แม่: Product
// ---------------------------------------------------------
class Product {
  // Properties
  String _id; // private (_) ตามโจทย์
  String name;
  double _price; // ใช้ _price เพื่อเก็บค่า และใช้ getter/setter คุม
  int? stock; // nullable (?) ตามโจทย์

  // Constructor
  Product({
    required String id,
    required this.name,
    required double price,
    this.stock,
  }) : _id = id,
       _price = price;

  // Getter: อ่านค่า _id
  String get id => _id;

  // Getter: อ่านค่า _price
  double get price => _price;

  // Setter: price
  set price(double newPrice) {
    if (newPrice > 0) {
      _price = newPrice;
    } else {
      print('ราคาไม่ถูกต้อง (ต้อง > 0) -> ไม่เปลี่ยนค่า');
    }
  }

  // Method: applyDiscount
  void applyDiscount(double percent) {
    if (percent >= 0 && percent <= 100) {
      _price = _price - (_price * (percent / 100));
    } else {
      print('เปอร์เซ็นต์ส่วนลดไม่ถูกต้อง');
    }
  }

  // Method: restock
  void restock(int amount) {
    stock = (stock ?? 0) + amount;
  }

  // Method: showInfo
  void showInfo() {
    print('ID: $_id');
    print('Name: $name');
    print('Price: $_price');
    print('Stock: ${stock ?? "ยังไม่ระบุสต็อก"}');
  }
}

// ---------------------------------------------------------
// 2) Class ลูก: DigitalProduct
// ---------------------------------------------------------
class DigitalProduct extends Product {
  double fileSizeMB;

  DigitalProduct({
    required String id,
    required String name,
    required double price,
    int? stock,
    required this.fileSizeMB,
  }) : super(id: id, name: name, price: price, stock: stock);

  @override
  void showInfo() {
    super.showInfo();
    print('Type: Digital');
    print('File Size: $fileSizeMB MB');
  }
}

// ---------------------------------------------------------
// 3) Class ลูก: FoodProduct
// ---------------------------------------------------------
class FoodProduct extends Product {
  String expireDate;

  FoodProduct({
    required String id,
    required String name,
    required double price,
    int? stock,
    required this.expireDate,
  }) : super(id: id, name: name, price: price, stock: stock);

  @override
  void showInfo() {
    super.showInfo();
    print('Type: Food');
    print('Expire Date: $expireDate');
  }
}


<img width="445" height="700" alt="image" src="https://github.com/user-attachments/assets/a22d484d-7dfc-4404-bc88-066043177380" />
